[package]
name = "nova-wallet"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Nova Chain wallet core (PHASE 1 placeholder)"

[lints]
workspace = true

[dependencies]
# PHASE 1：零第三方运行时依赖。
# HD Wallet / 密钥管理 / 签名方案待对应 PHASE + ADR 评审后引入。
