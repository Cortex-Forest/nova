[package]
name = "nova-execution"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Nova Chain WASM execution layer (PHASE 1 placeholder)"

[lints]
workspace = true

[dependencies]
# STEP 7G：状态转换引用协议类型（nova-core）与协议规则（nova-crypto：TransactionV1 / VerifyingKey）
nova-core = { path = "../core" }
nova-crypto = { path = "../crypto" }

[dev-dependencies]
# STEP 7G property tests：成功路径不变量 / 失败无副作用
proptest = "1"
