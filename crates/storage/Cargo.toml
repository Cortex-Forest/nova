[package]
name = "nova-storage"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Nova Chain storage layer (PHASE 1 placeholder)"

[lints]
workspace = true

[dependencies]
# STEP 8B-2：node encoding / domain-separated hashing 引用协议类型与 protocol_hash
nova-core = { path = "../core" }
nova-crypto = { path = "../crypto" }

[dev-dependencies]
# STEP 8B-2 property tests：node roundtrip / hash 一致性
proptest = "1"
